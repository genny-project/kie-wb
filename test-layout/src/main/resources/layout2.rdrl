import java.lang.Number;
import genny.QEvent;
import genny.QCmd;

rule "layout2"
	dialect "mvel"
	when
		evt : QEvent( event_type == "EVT_LAYOUT" , code == "layout2" )
	then
		QCmd fact0 = new QCmd();
		fact0.setCmd_type( "CMD_LAYOUT" );
		fact0.setCode( "layout2" );
		insert( fact0 );
end
